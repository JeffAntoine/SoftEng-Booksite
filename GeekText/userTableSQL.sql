USE geek_text;
CREATE TABLE users (
    username varchar(255),
    password varchar(255),
    fname varchar(255),
    lname varchar(255),
    address varchar(255),
    ccnum varchar(255),
    ccdate varchar(255),
    exaddress varchar(255),
    exccnum varchar(255),
    exccdate varchar(255)
);
